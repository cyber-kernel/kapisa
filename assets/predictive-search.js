document.addEventListener('DOMContentLoaded',()=>{const inputs=[...document.querySelectorAll('input[type="search"]')];inputs.forEach(input=>{let timer;input.addEventListener('input',()=>{clearTimeout(timer);const q=input.value.trim();if(q.length<2)return;timer=setTimeout(async()=>{try{const r=await fetch(`/search/suggest.json?q=${encodeURIComponent(q)}&resources[type]=product&resources[limit]=6`);const d=await r.json();let box=input.parentElement.querySelector('.predictive-results');if(!box){box=document.createElement('div');box.className='predictive-results';input.parentElement.appendChild(box);}box.innerHTML=(d.resources?.results?.products||[]).map(p=>`<a href="${p.url}"><span>${p.title}</span><small>${p.price}</small></a>`).join('')||'<span class="predictive-empty">No products found</span>';}catch(e){}},250);});});});